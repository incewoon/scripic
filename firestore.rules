rules_version = '2';
// All Firestore writes happen from Cloud Functions using the Admin SDK,
// which bypasses these rules. From the client we deny everything.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
